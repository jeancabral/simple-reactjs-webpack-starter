import React, { Component } from 'react';

export default class App extends Component {
  state = {};

  render() {
    return (
      <h1>
        A simple setup for ReajtJS from scratch with Babel, Webpack and Webpack Dev Server with
        ESLint, EditorConfig and Prettier tools
      </h1>
    );
  }
}
